# Streamlit Cloud: paste this into Settings -> Secrets (without the .example suffix).
# Locally: copy to .streamlit/secrets.toml — it is git-ignored.
API_URL = "https://insightmetric-api.onrender.com"
# API_KEY = "the-same-string-you-set-on-the-core"
